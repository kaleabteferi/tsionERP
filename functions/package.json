{
  "name": "tsion-erp-firebase-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,hosting",
    "deploy": "firebase deploy --only functions,hosting",
    "seed:firestore": "node scripts/seed-firestore.js",
    "seed:firestore:overwrite": "node scripts/seed-firestore.js --overwrite"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "exceljs": "^4.4.0",
    "express": "^4.21.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "multer": "^2.0.2",
    "pdfkit": "^0.17.1",
    "uuid": "^11.1.0"
  }
}
